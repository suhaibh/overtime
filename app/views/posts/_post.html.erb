<tr>
	<td><%= post.id %></td>
	<td><%= post.user.full_name %></td>
	<td><%= truncate(post.rationale) %></td>
	<td><%= post.date %> </td>
	<td><%= link_to "Edit", edit_post_path(post), id: "edit-post-#{post.id}" %></td>
</tr>